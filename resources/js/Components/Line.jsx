import React from 'react';

export const Line = ({ style }) => {
    return (
        <section style={style} className="line-wrap">
            <div className="container"></div>
        </section>
    );
};
