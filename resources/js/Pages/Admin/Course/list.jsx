import React from 'react';
import { AdminLayout } from '@/Layouts/AdminLayout';

const List = ({ rows, columns }) => {
    return (
        <AdminLayout rows={rows} columns={columns}>

        </AdminLayout>
    );
};

export default List;
